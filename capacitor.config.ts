import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.ionic.currencyconverter",
  appName: "currency-converter",
  webDir: "dist",
};

export default config;
